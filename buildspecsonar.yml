
version: 0.2
env:
  secrets-manager:
    LOGIN: 'c8a21323ebad51d4b24d0ffaa9b46c67521ded02'
    HOST: 'https://sonarcloud.io'
    Organization: 'piyush15081999'
    Project: 'Piyush_final_devops_project'
phases:
  install:
    commands:
      - echo "Install phase ..."
    runtime-versions:
      nodejs: 16
  pre_build:
    commands:
      - echo "Pre build phase ..."
      - apt-get update
      - apt-get install -y jq
      - wget https://binaries.sonarsource.com/Distribution/sonar-scanner-cli/sonar-scanner-cli-4.7.0.2747-linux.zip
      - echo "Current Path is >>>"
      - sh $pwd
      - unzip ./sonar-scanner-cli-4.7.0.2747-linux.zip
      - ls $pwd
      - export PATH=$PATH:./sonar-scanner-4.7.0.2747-linux/bin/
  build:
    commands:
      - echo "Build phase ..."
      - sonar-scanner -Dsonar.login=$LOGIN -Dsonar.host.url=$HOST -Dsonar.projectKey=$Project -Dsonar.organization=$Organization
      - sleep 5
      - curl https://sonarcloud.io/api/qualitygates/project_status?projectKey=$Project >result.json
      - cat results.json
      - if [$(jq -r '.projectStatus.status' result.json) = ERROR] ; then $CODEBUILD_BUILD_SUCCEDING -eq 0 :fi
  post_build:
    commands:
    - echo "Post build phase ..."      
